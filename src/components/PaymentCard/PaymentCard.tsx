
import "./PaymentCard.css";

interface Props {
    title: string 
    iconUrl: string
}

export const PaymentCard = ({ title, iconUrl }: Props) => {
    return (
        <div
        className="payment-card--container">
            
        </div>
    )
}